# Windows セットアップガイド (初心者向け)

PC に詳しくなくても順番通りにやれば動きます。所要時間 約 10 分。

---

## ステップ 1: Python をインストール

1. <https://www.python.org/downloads/windows/> を開く
2. **「Download Python 3.12.x」** などの黄色いボタンをクリック (3.10 以上であれば OK)
3. ダウンロードした `python-3.12.x-amd64.exe` を **ダブルクリック**
4. **必ず最初の画面で `Add python.exe to PATH` のチェックボックスにチェックを入れる** ← 重要！
5. `Install Now` をクリック
6. 完了したら `Close`

### 確認

「スタート」→ 検索欄に `cmd` と入力 → `コマンドプロンプト` を開き、以下を実行:

```
python --version
```

`Python 3.12.x` のように表示されれば OK。

---

## ステップ 2: このプロジェクトをダウンロード

### A. Git を使う場合 (推奨)

コマンドプロンプトで:

```
cd %USERPROFILE%\Documents
git clone <このリポジトリの URL> ALERT_Codex
cd ALERT_Codex
```

### B. Git を使わない場合

1. このリポジトリの GitHub ページで `Code` → `Download ZIP`
2. ダウンロードした ZIP を **右クリック → すべて展開**
3. 展開先を覚えておく (例: `C:\Users\ユーザ名\Documents\ALERT_Codex`)

---

## ステップ 3: セットアップを実行

`scripts` フォルダの中の **`setup.bat` をダブルクリック**

- 黒いウィンドウが開いて自動でセットアップが進みます
- 「セットアップ完了！」と表示されるまで数分待ちます
- 何かキーを押すとウィンドウが閉じます

---

## ステップ 4: ダッシュボードを起動

プロジェクトのトップ (`run.bat` がある場所) で **`run.bat` をダブルクリック**

- 黒いウィンドウが開きます (これがサーバ本体)
- 数秒後にブラウザが自動的に <http://127.0.0.1:8000> を開きます
- 開かない場合はブラウザのアドレス欄に手動で <http://127.0.0.1:8000> と入力

最初の 30〜60 秒はデータ取得中なので画面はほとんど空です。少し待つとイベントが流れ始めます。

### 停止するには

黒いウィンドウを **閉じる** (×ボタン) か、その中で `Ctrl + C` を押す。

---

## トラブルシューティング

### ❌ 「`python` は内部コマンドまたは外部コマンド…」と出る

→ ステップ 1 で **PATH へ追加** にチェックを入れずにインストールしてしまっています。
   Python を一度アンインストール → 再インストールし、必ず `Add python.exe to PATH` にチェックを入れてください。

### ❌ `setup.bat` を実行すると一瞬で消える

→ コマンドプロンプトを開いて手動で実行するとエラーが見えます:

```
cd C:\path\to\ALERT_Codex
scripts\setup.bat
```

### ❌ ブラウザを開いても「サイトに接続できません」

→ `run.bat` の黒いウィンドウに `Application startup complete.` が表示されていますか?
   表示されていない場合は、その上にあるエラーメッセージを確認してください。
   表示されていれば、ブラウザで強制リロード (Ctrl+F5) してみてください。

### ❌ イベントが全然出てこない

→ インターネット接続を確認してください。社内プロキシや厳格なファイアウォールの環境では一部の RSS が届かない場合があります。
   黒いウィンドウのログに `RSS fetch failed: ...` がいくつか出るのは正常です (一部のソースが届かなくても他のソースが使えれば OK)。

### ❌ Windows Defender SmartScreen に警告される

→ `setup.bat` / `run.bat` は単純なバッチファイルなので無害です。
   `詳細情報` → `実行` で進めてください。心配なら中身をメモ帳で開いて確認できます。

---

## ステップ 5 (任意): 設定をカスタマイズ

### 更新間隔を変える

`run.bat` をメモ帳で開き、最後の行を以下のように変更:

```bat
set WM_REFRESH_INTERVAL=300
".venv\Scripts\python.exe" -m backend.main
```

300 = 5 分間隔 (デフォルトは 600 = 10 分)。

### ソースを増やす / 減らす

`backend\config.py` をメモ帳で開いて `RSS_SOURCES` のリストを編集します。
不要なものは行頭に `#` を付けてコメントアウト。

---

## アンインストール

ALERT_Codex フォルダごと **ゴミ箱に入れて削除** すれば終わりです。
レジストリや C: ドライブに何も書き込みません。
